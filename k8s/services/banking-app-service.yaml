apiVersion: v1
kind: Service
metadata:
  name: banking-app-service
  namespace: default
  labels:
    app: banking-app
spec:
  type: NodePort
  selector:
    app: banking-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
